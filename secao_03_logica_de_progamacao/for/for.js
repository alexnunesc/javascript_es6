// Objetivo: Apresentar a estrutura de repetição FOR.
const array = ['one', 'two', 'three', 'four', 'five'];

for (let i = 0; i <= array.length -1; i += 1) {
  console.log(`Posição ${i} - ${array[i]}`);
}

